# MCP in one paragraph

The Model Context Protocol (MCP) is an open protocol that lets LLM applications connect to external tools and data through servers. A server can expose *tools* (actions the model can call), *resources* (data it can read) and *prompts* (reusable templates).
