# Git: unstage with git restore --staged

Since Git 2.23, `git restore --staged <file>` unstages a file and keeps your changes. `git restore <file>` (without `--staged`) discards working-tree changes, so use it carefully.
